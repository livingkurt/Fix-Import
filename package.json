{
  "name": "fix-imports",
  "version": "0.0.1",
  "displayName": "Fix Imports",
  "description": "Extension to fix imports",
  "main": "./out/extension.js",
  "engines": {
    "vscode": "^1.81.0"
  },
  "scripts": {
    "compile": "tsc"
  },
  "contributes": {
    "commands": [
      {
        "command": "extension.fixImports",
        "title": "Fix Imports"
      }
    ],
    "keybindings": [
      {
        "command": "extension.fixImports",
        "key": "ctrl+f9"
      }
    ]
  },
  "activationEvents": [
    "onCommand:extension.fixImports"
  ],
  "dependencies": {
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@types/vscode": "^1.81.0",
    "vscode-test": "^1.6.1"
  }
}
